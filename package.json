{
  "name": "BKFA",
  "version": "1.0.0",
  "description": "ahihi",
  "scripts": {
    "eslint": "cd client && npm run eslint && cd ../server && npm run eslint",
    "setup": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "client": "npm run dev:server --prefix client",
    "server": "npm run dev --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.2"
  }
}
